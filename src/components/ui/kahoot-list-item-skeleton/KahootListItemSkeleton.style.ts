import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 8,
    overflow: 'hidden',
    shadowColor: '#000',
    elevation: 4,
    flexDirection: 'row',
  },
  rightContainer: {
    padding: 8,
    backgroundColor: '#fff',
    justifyContent: 'space-between',
  },
});
